package lessons.bankingApp.service;

public class BankingService {/*
    public BankingService(){

    }
    public void withdrawn(Account account, double amount){
        double currentBalance = account.getBalance();
        if(currentBalance < amountToWithdraw){
            System.out.println(String.format("Amount to withdraw '%s' is greater than account '%s' balance '%s'",amountToWithdraw, account.getAccountNumber(), currentBalance));
        }
        double newBalance = currentBalance - amountToWithdraw;
        account.setBalance(newBalance);


     */


}
