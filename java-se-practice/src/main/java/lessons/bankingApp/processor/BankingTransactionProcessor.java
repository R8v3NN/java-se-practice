package lessons.bankingApp.processor;
/*
import lessons.bankingApp.parser.BankingTransactionCSVParser;
import lessons.bankingApp.parser.BankingTransactionParser;

import java.util.List;
/*
public class BankingTransactionProcessor {
    public static double getTotalAmountForMoneyTransferSend(List<BankingTransaction> transactions){
        String targetTransactionType = "Money to transfer";
        double total = 0;
        for(BankingTransaction tansaction: transactions){
            if(transaction.getTransactionType().equals(target.TransactionType)){
                total += transaction.getAmount;
            }
        }
        return total;
    }
}

 */